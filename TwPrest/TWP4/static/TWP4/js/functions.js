const randInt = (min, max)=>{
    return Math.floor(Math.random() * (max - min) ) + min;
}

const makeTriplet = n =>{
    const triplets = [
        [1, 2, 2],
        [1, 4, 8]
    ]
    return triplets[0]
}